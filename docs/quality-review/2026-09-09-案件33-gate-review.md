# 品質レビュー: 案件33 シグナル蓄積トリガー・レポート（マイルストーンナラティブ）

- 判定: CHANGES REQUIRED
- レビュー担当: Codex
- レビュー日: 2026-09-09
- 対象TASK: `TASK.md`「案件33: シグナル蓄積トリガー・レポート(マイルストーンナラティブ)」
- 対象コミット: `b3d80ff82fcf1d39052327ba1d241d8c9a1c2e1a`

## 判定の種類

判定は次の3種類だけを使う。

- `PASS`
  条件を満たし、次工程へ進める。
- `CHANGES REQUIRED`
  担当者へ具体的な修正を戻す。原則としてKimiへ差し戻す。
- `ESCALATE`
  Claudeまたはユーザーの判断が必要である。

## 記入のきまり

各チェック項目は、必ず次の3状態のどれか1つになる。理由欄は項目ごとに1つあり、
その直上の項目にだけ対応する。Gate全体をまとめた理由欄は使わない。

| 状態 | チェック欄 | その項目の「未適用の理由」 |
| --- | --- | --- |
| 確認済み | `[x]` | 空欄のままにする |
| 未適用（今回のタスクに当てはまらない） | `[ ]` | 必ず理由を書く |
| 未確認（まだ確認していない） | `[ ]` | 空欄 |

- 未チェックかつ未適用の理由が空欄の項目は「未確認」として扱う。未確認の項目が
  1つでも残っている場合、`PASS` にはできない。`CHANGES REQUIRED` または
  `ESCALATE` とし、何を確認すべきかを指摘事項に書く。
- 確認した結果として条件を満たしていない項目は、未適用ではない。チェックせず、
  理由欄には書かず、指摘事項へ内容を記載して `CHANGES REQUIRED` とする。
- 未適用の理由は、その項目が今回のタスクに当てはまらない根拠を書く。
  「不要」だけでは足りず、なぜ当てはまらないかを1文で書く。
- 項目番号（`1-1` など）は変更しない。第三者が、どの項目がどの状態なのかを
  項目番号で一意に追えるようにするためである。
- `PASS` と記録するには、Gate 1〜5の全項目が「確認済み」または
  「未適用（理由あり）」のいずれかで埋まっている必要がある。

## Gate 1 仕様

- [x] 1-1 目的、利用者、対象外、受入条件が明確である。
  - 1-1 未適用の理由:
- [x] 1-2 不明点が実装者の推測に委ねられていない。
  - 1-2 未適用の理由:

## Gate 2 設計

- [x] 2-1 必要な構成、データ、画面、エラー対応が決まっている。
  - 2-1 未適用の理由:
- [x] 2-2 DB、認証、課金、公開API、アーキテクチャ、大規模変更はClaudeが承認している。
  - 2-2 未適用の理由:

## Gate 3 実装

- [x] 3-1 テストを先に作り、失敗を確認してから実装している。
  - 3-1 未適用の理由:
- [ ] 3-2 宣言された担当範囲だけを変更している。
  - 3-2 未適用の理由:
- [x] 3-3 重要な処理には非エンジニアにも理解できる日本語の説明がある。
  - 3-3 未適用の理由:

## Gate 4 レビュー

- [ ] 4-1 関連テスト、型チェック、ビルドが成功する。
  - 4-1 未適用の理由:
- [x] 4-2 秘密情報、危険な権限、入力値、エラー処理を確認している。
  - 4-2 未適用の理由:
- [ ] 4-3 納品後に別担当者が保守できる構造になっている。
  - 4-3 未適用の理由:

## Gate 5 納品

- [ ] 5-1 README、環境構築手順、操作方法、既知の制約が揃っている。
  - 5-1 未適用の理由: 既存API・既存定期Workerへの追加で、新しい環境構築手順やユーザー操作、新規UIは発生せず、API挙動と既知の制約はTASK.mdおよび実装報告書に記録されているため。
- [ ] 5-2 引き継ぎノートと再利用可能な知見が更新されている。
  - 5-2 未適用の理由:
- [ ] 5-3 外部公開、課金、データ削除を含む場合は、実行前のユーザー承認を得ている。
  - 5-3 未適用の理由: 本コミットはローカル実装とテスト・文書更新のみで、外部公開、課金、データ削除を実行していないため。

## 実行した確認

- コマンド: `git diff --check b3d80ff^ b3d80ff`
- 結果: 成功。空白エラーなし。
- コマンド: `python -m pytest`（`backend` ディレクトリ）
- 結果: 成功。390 passed、6 warnings、77.87秒。警告は既存のStarlette/FastAPI非推奨警告と、サンドボックスにより `.pytest_cache` を作成できない警告。
- コマンド: `./gradlew testDebugUnitTest`
- 結果: 実行不能。既定のGradleキャッシュ `C:\.gradle` にロックファイル用親ディレクトリを作成できなかった。
- コマンド: `$env:GRADLE_USER_HOME='C:\Users\vinta\.gradle'; ./gradlew testDebugUnitTest --offline`
- 結果: 実行不能。サンドボックス制約により既存Gradleキャッシュの `gradle-9.3.1-bin.zip.lck` へ書き込めなかった。
- 確認できなかった範囲: Android/KMPのユニットテストとビルドの独立再実行。実装報告書には同コマンドの `BUILD SUCCESSFUL` が記録されているが、本レビュー環境では再確認できていない。

## 指摘事項

指摘は `対象箇所 / 問題 / 影響 / 必要な修正 / 再確認方法` の順序で記載する。

### 指摘1

- 対象箇所: `backend/discovery/router.py` の `get_milestone_narrative()`、`backend/discovery/repository.py` のマイルストーンキャッシュ保存処理
- 問題: キャッシュ確認とGemini生成の間が排他されていない。並行する2リクエストが同時にキャッシュ未作成を観測すると、DBには一意制約で1件だけ保存されてもGeminiは2回呼び出される。
- 影響: TASK.mdの「同一session_id×同一milestoneのキャッシュを必ず機能させ、Geminiの重複呼び出しを避ける」を満たさず、重複課金・応答遅延・レート制限消費が発生する。
- 必要な修正: session_id×milestone単位で生成権を一意に確保する仕組み（DB上の生成予約、適切な排他制御等）を導入し、敗者リクエストがGeminiを呼ばず勝者のキャッシュを再利用するようにする。
- 再確認方法: 同一session_id×milestoneへ並行リクエストするテストを追加し、両方が同一内容を返し、`generate_milestone_narrative()` の呼び出しが合計1回であることを確認する。

### 指摘2

- 対象箇所: `app/src/main/java/com/example/myapplication/work/DiscoveryPeriodicReportWorker.kt` のマイルストーン処理、`DiscoveryReportNotifier.kt` の通知文言
- 問題: Workerは現在の最新milestoneを1件だけ取得・保存するため、前回通知1から次回実行時3へ進んでいた場合にmilestone 2を生成・通知しない。また20件以降も通知タイトルが常に「シグナルが10件溜まりました」と表示される。
- 影響: 「InterestSignal累積件数が10の倍数に達するたびに新レポートを生成する」という受入条件を満たさず、中間マイルストーンが欠落し、表示件数も事実と異なる。
- 必要な修正: 未処理の各マイルストーンを欠落なく生成・記録する設計に直し、通知文言へ実際の到達件数（milestone×10）を反映する。APIを最新milestone取得のままとするか、対象milestone指定を追加するかは既存公開APIとの互換性を維持して決める。
- 再確認方法: 前回保存値1・現在30件のケースでmilestone 2と3が欠落なく処理されるテスト、および10/20/30件で通知文言が正しいテストを追加する。

### 指摘3

- 対象箇所: `app/src/main/java/com/example/myapplication/work/DiscoveryReportNotifier.kt` の `reportIdentityFor()`
- 問題: 既存の通知ID/requestCode式を `sessionId * 2 + 種別` から `sessionId * 3 + reportType.ordinal` へ変更しており、既存週次・月次のIDが変わる。`ordinal` はenumの並び替えでも意味が変化する。
- 影響: TASK.mdの「既存の週次/月次レポートのAPI契約・挙動は変更しない」に反し、アップグレード前後の既存通知/PendingIntentと衝突または意図しない置換が起こり得る。
- 必要な修正: 週次・月次の既存ID計算を維持し、MILESTONEだけ既存範囲と衝突しない安定した明示ID領域へ割り当てる。enum順序に依存しない値を用いる。
- 再確認方法: 変更前の週次・月次IDを固定値として回帰テストし、複数sessionの3種別が相互に衝突しないことも確認する。

### 指摘4

- 対象箇所: `backend/tests/test_discovery_repository.py`、`TASK.md` の案件33「実装結果」
- 問題: 案件33と無関係な `behavior_categories` 既存DBマイグレーションテストが本コミットへ混入し、同趣旨のテストクラスも重複している。また共通AGENTS.mdが要求する所定の「テスト結果」へのコマンド記録と「引き継ぎメモ」の完了事項・次担当・次行動が更新されていない。
- 影響: 担当範囲とコミットの原子性が崩れ、後続担当者が案件33の変更理由・検証履歴・次作業を正確に追跡しにくい。
- 必要な修正: 無関係な重複テストを案件33コミットから除外するか由来を明確化し、TASK.mdの所定欄へ実行コマンド、結果、完了事項、次担当、次行動を追記する。
- 再確認方法: `git diff b3d80ff^..修正後` の変更一覧が案件33の担当範囲に限定され、TASK.mdの「テスト結果」「引き継ぎメモ」に必要事項が揃っていることを確認する。

### 指摘5

- 対象箇所: Android/KMP全体の検証
- 問題: 本レビューのサンドボックスではGradleキャッシュへのロックファイル書き込みが拒否され、`./gradlew testDebugUnitTest` を独立再実行できていない。
- 影響: 実装報告書上は成功しているが、Gate 4-1を本レビュー環境で再現確認できず、修正後のAndroid/KMP回帰も未確認となる。
- 必要な修正: 書き込み可能なGradleキャッシュを利用できる環境で、指定テストと必要なビルドを再実行する。
- 再確認方法: `./gradlew testDebugUnitTest`（必要に応じて `./gradlew :app:compileDebugAndroidTestKotlin`）が `BUILD SUCCESSFUL` になることをログで確認する。

## 次の行動

- 差し戻し先（原則としてKimi）: Kimi
- 期限: 次回品質ゲート再レビュー前
- エスカレーション先（必要な場合のみ。設計はClaude、予算・納期・仕様はユーザー）: 複数マイルストーンを跨いだ場合の生成・通知方式、または公開API変更が必要になる場合はClaude

判定: CHANGES REQUIRED

# 品質レビュー: 案件32 Googleアカウント連携（再レビュー）

- レビュー担当: Codex
- レビュー日: 2026-09-09
- 対象TASK: `TASK.md` の「案件32: Googleアカウント連携(設定画面「アカウントを作成」の実装)」
- 比較基点: `8efd5b7` (前回ゲートレビュー記録)
- 対象コミット: `b94dfe5`, `77d23f0`

## Gate 1 仕様

- [x] 1-1 目的、利用者、対象外、受入条件が明確である。
  - 1-1 未適用の理由:
- [x] 1-2 不明点が実装者の推測に委ねられていない。
  - 1-2 未適用の理由:

## Gate 2 設計

- [x] 2-1 必要な構成、データ、画面、エラー対応が決まっている。
  - 2-1 未適用の理由:
- [x] 2-2 DB、認証、課金、公開API、アーキテクチャ、大規模変更はClaudeが承認している。
  - 2-2 未適用の理由:

## Gate 3 実装

- [ ] 3-1 テストを先に作り、失敗を確認してから実装している。
  - 3-1 未適用の理由:
- [x] 3-2 宣言された担当範囲だけを変更している。
  - 3-2 未適用の理由:
- [x] 3-3 重要な処理には非エンジニアにも理解できる日本語の説明がある。
  - 3-3 未適用の理由:

## Gate 4 レビュー

- [x] 4-1 関連テスト、型チェック、ビルドが成功する。
  - 4-1 未適用の理由:
- [x] 4-2 秘密情報、危険な権限、入力値、エラー処理を確認している。
  - 4-2 未適用の理由:
- [x] 4-3 納品後に別担当者が保守できる構造になっている。
  - 4-3 未適用の理由:

## Gate 5 納品

- [x] 5-1 README、環境構築手順、操作方法、既知の制約が揃っている。
  - 5-1 未適用の理由:
- [x] 5-2 引き継ぎノートと再利用可能な知見が更新されている。
  - 5-2 未適用の理由:
- [ ] 5-3 外部公開、課金、データ削除を含む場合は、実行前のユーザー承認を得ている。
  - 5-3 未適用の理由: 今回の開発・レビューで外部公開、課金、実ユーザーデータの削除操作は実行していない。連携解除機能は、利用者自身の行操作で端末内プロフィールを削除する製品機能である。

## 実行した確認

- コマンド: `git rev-parse 8efd5b7`; `git log 8efd5b7..HEAD --oneline`; `git diff --stat 8efd5b7...HEAD`; `git diff --check 8efd5b7...HEAD`; `git diff --name-status 8efd5b7...HEAD`
- 結果: 比較基点を `8efd5b70a235ae8f2559cd1b6662db2b09b8c7fa` に固定。対象は `b94dfe5` と `77d23f0`、11ファイル、726行追加・76行削除で、空白エラーなし。バックエンド、DBスキーマ、既存カレンダー認可実装の差分なし。
- コマンド: `git diff 8efd5b7...HEAD` と対象ソース／テストの行単位照合
- 結果: 前回指摘1のActivity context伝播、指摘2のUIからの連携解除と保存値削除、指摘3のstate全体伝播・エラー分類・保存復元解除テストは確認。一方、通常の `Linked(displayName, email, photoUrl)` で `photoUrl` が画面に表示されず、該当UIテストもない。
- コマンド: `Get-ChildItem` とXML集計による `app/build/test-results/testDebugUnitTest` / `shared/build/test-results/testDebugUnitTest` および `app/build/outputs/apk/debug/app-debug.apk` の確認
- 結果: 2026-09-09生成のテスト結果は `app` 128件、`shared` 201件で failures=0 / errors=0。debug APK（23,501,457 bytes）の生成も確認。`77d23f0` には `--rerun-tasks` による3タスクの `BUILD SUCCESSFUL` 記録あり。
- コマンド: `.\gradlew.bat :shared:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug --rerun-tasks --stacktrace`
- 結果: サンドボックスの既定Gradle homeが書き込み不可の `C:\.gradle` を指し、wrapper lockの親ディレクトリを作成できず失敗。
- コマンド: ワークスペース内Gradle home、ローカルGradle 9.3.1、`--offline` を使った同3タスクの再実行
- 結果: タスク構成前に `org.gradle.toolchains.foojay-resolver-convention:1.0.0` をオフラインキャッシュから解決できず失敗。対象コードの新規コンパイル・テスト実行には到達していない。
- 確認できなかった範囲: このサンドボックスでの3 Gradleタスクの新規完走、Credential Managerの実機UI、TDDのRED実行履歴。

## 前回指摘1〜3の再判定

- 指摘1: **解消**。`MainActivity.kt` が `this@MainActivity` を `signIn()` へ渡し、`AndroidGoogleCredentialProvider.getCredential()` がその前面Activity contextを使用する。
- 指摘2: **解消**。`Linked` 行から `LocalGoogleAccountSignOutHandler` を通じて `signOut()` へ到達し、3つの保存値削除と `NotLinked` 遷移をテストしている。
- 指摘3: **部分解消**。state全体の伝播、`displayName == null` の `Linked` 判定、`NotConfigured`、失敗・キャンセル分類、保存・復元・解除テストは追加済み。ただし `photoUrl` の通常時表示とそのUIテストが欠け、RED実行証跡も確認できない。

## 指摘事項

### 指摘1

- 対象箇所: `shared/src/commonMain/kotlin/com/example/myapplication/shared/ui/discovery/SettingsTabScreen.kt:92-98`, `app/src/test/java/com/example/myapplication/discovery/SettingsTabGoogleAccountLinkJvmTest.kt:84-103`
- 問題: `displayName` と `email` が存在する通常の連携結果では、サブタイトルに `displayName`、説明に `email` を選ぶため `photoUrl` が画面に表示されない。テストも表示名とメールのみを検証している。
- 影響: `TASK.md` の「displayName/email/photoUrlを取得して設定画面に表示」および前回指摘3の「email/photoUrlも表示」を満たさず、指摘3が完全に解消していない。
- 必要な修正: `Linked` の3値がすべて存在する場合でも `photoUrl` が利用者に確認できる表示を追加する。仕様がURL文字列ではなくプロフィール画像の表示を意図する場合は、画像として表示する。
- 再確認方法: `Linked(displayName, email, photoUrl)` の3値がすべて存在するケースで `photoUrl` の表示（または画像ノード）を検証するUIテストを追加し、関連テストと `:app:assembleDebug` を実行する。

### 指摘2

- 対象箇所: `b94dfe5`, `TASK.md:5581`, `docs/quality-review/2026-09-09-案件32-google-account-link-report.md:112-119`
- 問題: 修正テストと実装は同一コミットに含まれ、報告は「TDDで網羅」とするだけで、実装前に失敗を確認したREDのコマンドと結果が記録されていない。
- 影響: AGENTS.mdと案件32の「TDD必須」およびGate 3-1を客観的に確認できない。テスト網羅自体の改善は確認できる。
- 必要な修正: 残る `photoUrl` 表示不足を、失敗するUIテストを先に追加・実行してREDを記録した後、実装してGREENを記録する。
- 再確認方法: レビュー報告で対象テストのRED/GREENのコマンド、失敗理由、成功結果を確認し、実装差分と照合する。

## 次の行動

- 差し戻し先（原則としてKimi）: Kimi
- 期限: 未設定
- エスカレーション先（必要な場合のみ。設計はClaude、予算・納期・仕様はユーザー）: `photoUrl` をURL文字列と画像のどちらで表示するかの設計判断が必要になった場合はClaude。
